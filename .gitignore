# Root level ignores
.DS_Store
*.log
.env
.env.local
.env.*.local

# Docker
docker-compose.override.yml

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
Thumbs.db

# Database dumps are kept OUT of git — they contain user PII
# (emails, password hashes, chat). Store them somewhere durable instead
# (cloud / external drive). See db-backup/README.md.
db-backup/*.sql

